; Batas unggahan PHP untuk mode FPM/CGI (Hostinger, LiteSpeed, dan sebagian besar
; shared hosting). Nilainya harus DI ATAS batas terbesar yang divalidasi aplikasi —
; impor roster & data karyawan menerima 10 MB (max:10240) — supaya yang menolak
; berkas kebesaran adalah validasi Laravel dengan pesannya yang terbaca, bukan
; server dengan halaman 413 telanjang sebelum PHP sempat berjalan.
;
; post_max_size dibuat lebih besar dari upload_max_filesize karena body permintaan
; ikut memuat token CSRF dan medan lain di samping berkasnya.
upload_max_filesize = 16M
post_max_size = 20M
max_file_uploads = 20
